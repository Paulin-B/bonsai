# Required
PyQt6>=6.6
requests>=2.31
beautifulsoup4>=4.12
Pillow>=10.0

# Optional, each enabling one feature. Bonsai runs without all of them.
pyspellchecker>=0.8      # red underlines in the input box
PyYAML>=6.0              # syntax checking for .yaml files
tree-sitter>=0.23        # syntax checking and reference tracking for ~40 languages
tree-sitter-language-pack>=1.0
websockets>=12.0         # the Neuro API game link, so games can drive Bonsai directly
# Speech is optional and lives in its own virtual environment, because the system
# Python is often externally managed and a 300MB network does not belong in it:
#   python3 -m venv ~/.local/share/bonsai-voice
#   ~/.local/share/bonsai-voice/bin/pip install kokoro-onnx faster-whisper
# then put kokoro.onnx and voices.bin in ~/.local/share/bonsai_voices/.
# piper or espeak-ng on PATH also work for speaking, and need none of that.
# faster-whisper is what lets it hear you; its model downloads on first use.
